# docker/dagster/Dockerfile
#
# Dagster webserver + daemon image for the GitHub ETL pipeline.
#
# TODO: Implement when Dagster service is ready to containerize.
# For now, Dagster is run locally with `dagster dev`.
#
# Expected build steps:
#   1. FROM python:3.12-slim
#   2. Install system dependencies (git, libpq-dev)
#   3. COPY pyproject.toml and install dependencies
#   4. COPY project source
#   5. Set DAGSTER_HOME environment variable
#   6. EXPOSE 3000 (webserver) and configure daemon entrypoint

FROM python:3.12-slim

# Placeholder — not yet implemented
CMD ["echo", "Dagster Dockerfile not yet configured. Run 'dagster dev' locally."]
